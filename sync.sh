#echo "input: $INPUT_WHO_TO_GREET"
#name="sub-test"
#url="git@github.com:Talal-l/sub-test.git"
##url="git@p.github.com:Talal-l/sub-test.git"

#git subtree split --prefix $name -b $name # create a local  branch containing the splitted output folder
#git push  -f $url $name:main # force the push of the gh-pages branch to the remote gh-pages branch at origin
#git branch -D $name # delete the local gh-pages because you will need it: ref


#git subtree push --prefix=$name $url main
